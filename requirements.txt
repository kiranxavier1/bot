# Core trading infrastructure
ccxt[pro]>=4.3.0            # CCXT Pro for WebSocket + REST exchange access
aiohttp>=3.9.0              # Async HTTP (used internally by ccxt.pro)

# AI / LLM
anthropic>=0.30.0             # Anthropic AI SDK (for Claude)

# Technical analysis
pandas>=2.1.0               # DataFrame manipulation for candle data
numpy>=1.26.0               # Numerical operations for trendline math
# pandas-ta removed — ADX/RSI implemented natively in utils/indicators.py

# Environment & configuration
python-dotenv>=1.0.0        # Load secrets from .env file

# Notifications
httpx>=0.27.0               # Async HTTP for Telegram Bot API calls

# Logging
colorlog>=6.8.0             # Coloured console output

# Crypto news (optional news filter)
requests>=2.31.0            # Sync HTTP for CryptoPanic news API

# Web dashboard
fastapi>=0.111.0            # ASGI web framework for the dashboard
uvicorn[standard]>=0.29.0   # ASGI server (runs inside the asyncio event loop)
websockets>=12.0            # WebSocket support (used by uvicorn[standard])
